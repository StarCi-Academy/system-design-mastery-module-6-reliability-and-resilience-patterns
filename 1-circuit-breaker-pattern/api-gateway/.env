# App runtime
# (EN: App runtime)
PORT=3000

# Inventory service endpoint from host-run mode.
# (EN: Inventory service endpoint for host-run mode.)
INVENTORY_BASE_URL=http://localhost:3001
INVENTORY_TIMEOUT_MS=2000

# Circuit Breaker thresholds (opossum).
# (EN: Circuit Breaker thresholds for opossum.)
CB_TIMEOUT_MS=2500
CB_ERROR_THRESHOLD_PERCENTAGE=50
CB_RESET_TIMEOUT_MS=5000
